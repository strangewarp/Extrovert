
-- Test more thoroughly: Record, TestOrTrackNote, ChanErase, and Erase

-- Test more thoroughly: MIDI-IN

-- Add a saveSequences function.

-- In the saveSequences function, a sequence's save-loop should halt after reaching the seq's ".total" tick

-- In saveSequences function, if no savefile name was entered, check for "default"+number+".mid", and save in the first empty spot

-- Add a save-keyboard-command

-- Add a savefile-name box, with manual save override

-- Add Pd number-boxes for BPM and TPQ
