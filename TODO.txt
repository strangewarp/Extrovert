
-- Update all GUI changes, including full big-endianness
	. Remove humanization
	. Note-entry keys ("p")
	. Random-velocity-toggle key ("r")
	. Octave keys ("o")
	. Hold-to-move-absolute-slot key ("M") and its chords ("R" lower, "C" higher).
		> Replace old absolute-swap mechanism
	. Update all boundaries for all values
	. Change quantization mechanism to round(beat/q)
	. Change duration mechanism to ceil((beat/q)/d)

-- Make "Save" and "Load" buttons on the Pd canvas less accident-prone

-- Test:
	. MIDI-IN
	. Groove Mode GATE-chord position toggling
	. absolute-swap up and down
	. new changes to hotseat-tabbing
	. new changes to loadMidiFile
	. all of saveMidiFile

-- Remove all "debug" routines

-- Double-check contents of tutorial in README
	. Remove pre-revision Groove Mode info

-- Change Github tagline to "MIDI-beatslicing sequencer, for composition and live performance on the Monome."

-- Delete TODO file
